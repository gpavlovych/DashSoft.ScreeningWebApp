'use strict';

describe('screeningApp.parent module', function() {

  beforeEach(module('screeningApp.parent'));

  describe('parent controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var parentCtrl = $controller('ParentCtrl');
      expect(parentCtrl).toBeDefined();
    }));

  });
});