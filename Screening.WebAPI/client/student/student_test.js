'use strict';

describe('screeningApp.student module', function() {

  beforeEach(module('screeningApp.student'));

  describe('student controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var studentCtrl = $controller('StudentCtrl');
      expect(studentCtrl).toBeDefined();
    }));

  });
});